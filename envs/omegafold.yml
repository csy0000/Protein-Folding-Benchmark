name: omegafold
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - git
  - wget
  - numpy
  - biopython
  - pip:
      - torch
      # Install the cloned repository after environment creation:
      # python -m pip install -e models/OmegaFold
